# Results

LeanFT HTML reports and screenshots are generated here at runtime.

This folder is intentionally kept in source control (with only this placeholder).
Execution output files are excluded via `.gitignore`.
